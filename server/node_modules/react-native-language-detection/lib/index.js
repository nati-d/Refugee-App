import { NativeModules, Platform } from 'react-native';
const detectLanguage = async (original) => {
    switch (Platform.OS) {
        case 'ios':
            return await NativeModules.RNLanguageDetectionModule.detection(original);
        case 'android':
            return (await NativeModules.RNLanguageDetectionModule.detection(original)).filter(lang => lang.language !== 'und');
        default:
            console.error('Platform not supported');
            return Promise.reject();
    }
};
export default detectLanguage;
//# sourceMappingURL=index.js.map